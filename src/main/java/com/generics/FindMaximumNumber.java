package com.generics;

public class FindMaximumNumber {

    public static void main (String[] args) {
        System.out.println("Welcome to Generics");
    }

   /* public static Integer DisplayIntegerMaximumNo (Integer x, Integer y, Integer z) {

        Integer max = x;

        if (y.compareTo(max) > 0) {
            max = y;
        }

        if (z.compareTo(max) > 0) {
            max = z;
        }

        System.out.println("Maximim no:" + max);
        return max;
    }

    public static Double DisplayDoubleMaximumNo (Double x, Double y, Double z) {

        Double max = x;

        if (y.compareTo(max) > 0) {
            max = y;
        }

        if (z.compareTo(max) > 0) {
            max = z;
        }


        System.out.println(max);
        return max;
    }

    public static String DisplayStringMaximumNo (String x, String y, String z) {

        String max = x;

        if (y.compareTo(max) > 0) {
            max = y;
        }

        if (z.compareTo(max) > 0) {
            max = z;
        }

        System.out.println(max);
        return max;
    }*/
}
